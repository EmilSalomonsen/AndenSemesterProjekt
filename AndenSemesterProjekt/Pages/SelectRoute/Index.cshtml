@page
@model AndenSemesterProjekt.Pages.SelectRoute.IndexModel
@{
    ViewData["Title"] = "SelectRoute";
}

<form>
    <p>
        Search: <input type="text" asp-for="FilterCriteria" />
        <input type="submit" value="Filter" />
    </p>
</form>

@*<div class="container">
    <div class="row">
        @foreach (var r in Model.Routes)
            {
                <div class="col-sm">
                    <div class="card" style="width: 30rem;">
                        <img class="card-img-top" src="~/Image/@r.ImageName" alt="Card iamge cap" />
                        <div class="card-body">
                            <h5 class="card-title">@r.RouteId</h5>
                        </div>
                </div>
            }
    </div>
</div>*@